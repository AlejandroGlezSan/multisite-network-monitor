# Minimal NetFlow collector config (nfdump / nfacct style)
listen: 2055
protocol: netflow_v5
storage:
  type: memory
  retention: 7d
outputs:
  prometheus:
    enabled: true
    listen: 9600
    metrics_path: /metrics
