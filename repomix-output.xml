This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gitignore
backend/core/__init__.py
backend/core/asgi.py
backend/core/settings.py
backend/core/urls.py
backend/core/wsgi.py
backend/Dockerfile
backend/manage.py
backend/monitoring/admin.py
backend/monitoring/apps.py
backend/monitoring/migrations/__init__.py
backend/monitoring/migrations/0001_initial.py
backend/monitoring/models.py
backend/monitoring/serializers.py
backend/monitoring/tests.py
backend/monitoring/views.py
backend/requirements.txt
docker-compose.yml
monitoring-agents/netflow-agent/collector.conf
monitoring-agents/netflow-agent/Dockerfile
monitoring-agents/snmp-agent/Dockerfile
monitoring-agents/snmp-agent/README.md
monitoring-agents/snmp-agent/snmp_exporter.yml
monitoring-agents/voip-agent/Dockerfile
monitoring-agents/voip-agent/voip_exporter.yml
prometheus/alert_manager.yml
prometheus/alert_rules/alert_rules.yml
prometheus/prometheus.yml
README.md
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="backend/monitoring/admin.py">
from django.contrib import admin
from .models import Site, NetworkDevice, Metric, Alert


@admin.register(Site)
class SiteAdmin(admin.ModelAdmin):
    list_display = ['name', 'city', 'country', 'status', 'last_check', 'created_at']
    list_filter = ['status', 'country', 'city']
    search_fields = ['name', 'address', 'city']
    readonly_fields = ['created_at', 'updated_at', 'last_check']


@admin.register(NetworkDevice)
class NetworkDeviceAdmin(admin.ModelAdmin):
    list_display = ['name', 'site', 'device_type', 'ip_address', 'vendor', 'is_monitored', 'last_seen']
    list_filter = ['device_type', 'is_monitored', 'site']
    search_fields = ['name', 'ip_address', 'vendor', 'model', 'serial_number']
    readonly_fields = ['created_at', 'updated_at', 'last_seen']


@admin.register(Metric)
class MetricAdmin(admin.ModelAdmin):
    list_display = ['device', 'metric_type', 'value', 'unit', 'timestamp']
    list_filter = ['metric_type']
    search_fields = ['device__name']
    readonly_fields = ['timestamp']


@admin.register(Alert)
class AlertAdmin(admin.ModelAdmin):
    list_display = ['title', 'severity', 'status', 'site', 'device', 'detected_at']
    list_filter = ['severity', 'status']
    search_fields = ['title', 'description']
    readonly_fields = ['detected_at', 'acknowledged_at', 'resolved_at']
</file>

<file path="backend/monitoring/apps.py">
from django.apps import AppConfig


class MonitoringConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'monitoring'
    verbose_name = 'Network Monitoring'
</file>

<file path="backend/monitoring/tests.py">
from django.test import TestCase
from django.contrib.auth.models import User
from rest_framework.test import APIClient
from rest_framework import status
from .models import Site, NetworkDevice, Metric, Alert


class SiteModelTest(TestCase):
    def setUp(self):
        self.site = Site.objects.create(
            name="Sede Central",
            address="Calle Mayor 1",
            city="Las Palmas de GC",
            status="up"
        )

    def test_site_str(self):
        self.assertIn("Sede Central", str(self.site))

    def test_site_default_status(self):
        site = Site.objects.create(name="Nueva Sede", address="X", city="Y")
        self.assertEqual(site.status, 'unknown')


class NetworkDeviceModelTest(TestCase):
    def setUp(self):
        self.site = Site.objects.create(name="Sede", address="X", city="Y")
        self.device = NetworkDevice.objects.create(
            name="Router-01",
            site=self.site,
            device_type="router",
            ip_address="192.168.1.1"
        )

    def test_device_str(self):
        self.assertIn("192.168.1.1", str(self.device))

    def test_device_default_monitored(self):
        self.assertTrue(self.device.is_monitored)


class SiteAPITest(TestCase):
    def setUp(self):
        self.client = APIClient()
        self.site = Site.objects.create(name="Sede API", address="X", city="Y")

    def test_list_sites(self):
        response = self.client.get('/api/v1/sites/')
        self.assertEqual(response.status_code, status.HTTP_200_OK)

    def test_create_site(self):
        data = {"name": "Nueva", "address": "Calle 1", "city": "Madrid"}
        response = self.client.post('/api/v1/sites/', data)
        self.assertEqual(response.status_code, status.HTTP_201_CREATED)


class AlertAPITest(TestCase):
    def setUp(self):
        self.client = APIClient()
        self.site = Site.objects.create(name="Sede", address="X", city="Y")
        self.alert = Alert.objects.create(
            title="CPU alta",
            description="CPU al 95%",
            severity="critical",
            site=self.site
        )

    def test_acknowledge_alert(self):
        response = self.client.post(f'/api/v1/alerts/{self.alert.id}/acknowledge/')
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        self.alert.refresh_from_db()
        self.assertEqual(self.alert.status, 'acknowledged')

    def test_resolve_alert(self):
        response = self.client.post(f'/api/v1/alerts/{self.alert.id}/resolve/')
        self.assertEqual(response.status_code, status.HTTP_200_OK)
        self.alert.refresh_from_db()
        self.assertEqual(self.alert.status, 'resolved')
</file>

<file path="backend/monitoring/views.py">
from rest_framework import viewsets, filters
from rest_framework.decorators import action
from rest_framework.response import Response
from django.utils import timezone
from .models import Site, NetworkDevice, Metric, Alert
from .serializers import SiteSerializer, NetworkDeviceSerializer, MetricSerializer, AlertSerializer


class SiteViewSet(viewsets.ModelViewSet):
    """
    API endpoint para gestionar sedes (sites).
    Soporta filtrado por status y ordenación.
    """
    queryset = Site.objects.all()
    serializer_class = SiteSerializer
    filter_backends = [filters.SearchFilter, filters.OrderingFilter]
    search_fields = ['name', 'city', 'address']
    ordering_fields = ['name', 'status', 'created_at']

    @action(detail=True, methods=['get'])
    def devices(self, request, pk=None):
        """Devuelve los dispositivos asociados a una sede."""
        site = self.get_object()
        devices = site.devices.all()
        serializer = NetworkDeviceSerializer(devices, many=True)
        return Response(serializer.data)

    @action(detail=True, methods=['get'])
    def alerts(self, request, pk=None):
        """Devuelve las alertas activas de una sede."""
        site = self.get_object()
        alerts = Alert.objects.filter(site=site, status='active')
        serializer = AlertSerializer(alerts, many=True)
        return Response(serializer.data)


class NetworkDeviceViewSet(viewsets.ModelViewSet):
    """
    API endpoint para gestionar dispositivos de red.
    """
    queryset = NetworkDevice.objects.select_related('site').all()
    serializer_class = NetworkDeviceSerializer
    filter_backends = [filters.SearchFilter, filters.OrderingFilter]
    search_fields = ['name', 'ip_address', 'vendor', 'model']
    ordering_fields = ['name', 'device_type', 'created_at']

    def get_queryset(self):
        qs = super().get_queryset()
        site_id = self.request.query_params.get('site')
        device_type = self.request.query_params.get('type')
        if site_id:
            qs = qs.filter(site_id=site_id)
        if device_type:
            qs = qs.filter(device_type=device_type)
        return qs

    @action(detail=True, methods=['get'])
    def metrics(self, request, pk=None):
        """Devuelve las últimas métricas de un dispositivo."""
        device = self.get_object()
        limit = int(request.query_params.get('limit', 50))
        metrics = device.metrics.all()[:limit]
        serializer = MetricSerializer(metrics, many=True)
        return Response(serializer.data)


class MetricViewSet(viewsets.ModelViewSet):
    """
    API endpoint para métricas de dispositivos.
    """
    queryset = Metric.objects.select_related('device').all()
    serializer_class = MetricSerializer
    filter_backends = [filters.OrderingFilter]
    ordering_fields = ['timestamp', 'value']

    def get_queryset(self):
        qs = super().get_queryset()
        device_id = self.request.query_params.get('device')
        metric_type = self.request.query_params.get('type')
        if device_id:
            qs = qs.filter(device_id=device_id)
        if metric_type:
            qs = qs.filter(metric_type=metric_type)
        return qs


class AlertViewSet(viewsets.ModelViewSet):
    """
    API endpoint para alertas del sistema.
    """
    queryset = Alert.objects.select_related('site', 'device', 'acknowledged_by').all()
    serializer_class = AlertSerializer
    filter_backends = [filters.SearchFilter, filters.OrderingFilter]
    search_fields = ['title', 'description']
    ordering_fields = ['detected_at', 'severity', 'status']

    def get_queryset(self):
        qs = super().get_queryset()
        status = self.request.query_params.get('status')
        severity = self.request.query_params.get('severity')
        if status:
            qs = qs.filter(status=status)
        if severity:
            qs = qs.filter(severity=severity)
        return qs

    @action(detail=True, methods=['post'])
    def acknowledge(self, request, pk=None):
        """Marca una alerta como reconocida."""
        alert = self.get_object()
        alert.status = 'acknowledged'
        alert.acknowledged_at = timezone.now()
        alert.acknowledged_by = request.user if request.user.is_authenticated else None
        alert.save()
        return Response({'status': 'acknowledged', 'id': alert.id})

    @action(detail=True, methods=['post'])
    def resolve(self, request, pk=None):
        """Marca una alerta como resuelta."""
        alert = self.get_object()
        alert.status = 'resolved'
        alert.resolved_at = timezone.now()
        alert.save()
        return Response({'status': 'resolved', 'id': alert.id})
</file>

<file path=".gitignore">
# --- Python & Entornos Virtuales ---
venv/
.venv/
env/
bin/
lib/
include/
*.py[cod]
*$py.class
__pycache__/

# --- Django ---
db.sqlite3
db.sqlite4
static/
media/
staticfiles/
.webassets-cache

# --- Variables de Entorno (¡CRÍTICO!) ---
.env
.env.example
.env.local
.env.*.local

# --- Bases de Datos y Series Temporales ---
*.db
*.sql
*.sqlite
/prometheus/data/
/grafana/data/
/redis/data/
/postgres/data/

# --- Logs y Reportes ---
*.log
logs/
reports/*.pdf
reports/*.xlsx
reports/*.csv
/docs/_build/

# --- Docker & Infraestructura ---
.docker/
docker-compose.override.yml
*.tar
*.tar.gz

# --- Frontend (Node/React/Vue) ---
frontend/node_modules/
frontend/dist/
frontend/build/
.eslintcache
.stylelintcache

# --- Herramientas de Sistema y Editores ---
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.bak
*.tmp
</file>

<file path="backend/core/__init__.py">

</file>

<file path="backend/core/asgi.py">
"""
ASGI config for core project.

It exposes the ASGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/howto/deployment/asgi/
"""

import os

from django.core.asgi import get_asgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'core.settings')

application = get_asgi_application()
</file>

<file path="backend/core/wsgi.py">
"""
WSGI config for core project.

It exposes the WSGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/howto/deployment/wsgi/
"""

import os

from django.core.wsgi import get_wsgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'core.settings')

application = get_wsgi_application()
</file>

<file path="backend/manage.py">
#!/usr/bin/env python
"""Django's command-line utility for administrative tasks."""
import os
import sys


def main():
    """Run administrative tasks."""
    os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'core.settings')
    try:
        from django.core.management import execute_from_command_line
    except ImportError as exc:
        raise ImportError(
            "Couldn't import Django. Are you sure it's installed and "
            "available on your PYTHONPATH environment variable? Did you "
            "forget to activate a virtual environment?"
        ) from exc
    execute_from_command_line(sys.argv)


if __name__ == '__main__':
    main()
</file>

<file path="backend/monitoring/migrations/__init__.py">

</file>

<file path="backend/monitoring/migrations/0001_initial.py">
# Generated by Django 4.2.7 on 2026-03-11 20:52

from django.conf import settings
from django.db import migrations, models
import django.db.models.deletion


class Migration(migrations.Migration):

    initial = True

    dependencies = [
        migrations.swappable_dependency(settings.AUTH_USER_MODEL),
    ]

    operations = [
        migrations.CreateModel(
            name='Site',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('name', models.CharField(max_length=200)),
                ('description', models.TextField(blank=True)),
                ('address', models.CharField(max_length=500)),
                ('city', models.CharField(max_length=100)),
                ('country', models.CharField(default='España', max_length=100)),
                ('latitude', models.DecimalField(blank=True, decimal_places=6, max_digits=9, null=True)),
                ('longitude', models.DecimalField(blank=True, decimal_places=6, max_digits=9, null=True)),
                ('ip_range', models.CharField(blank=True, help_text='Ej: 192.168.1.0/24', max_length=100)),
                ('vlan_id', models.IntegerField(blank=True, null=True)),
                ('status', models.CharField(choices=[('up', 'Operacional'), ('warning', 'Advertencia'), ('down', 'Inactivo'), ('unknown', 'Desconocido')], default='unknown', max_length=20)),
                ('last_check', models.DateTimeField(blank=True, null=True)),
                ('contact_name', models.CharField(blank=True, max_length=100)),
                ('contact_phone', models.CharField(blank=True, max_length=20)),
                ('contact_email', models.EmailField(blank=True, max_length=254)),
                ('created_at', models.DateTimeField(auto_now_add=True)),
                ('updated_at', models.DateTimeField(auto_now=True)),
            ],
            options={
                'ordering': ['name'],
            },
        ),
        migrations.CreateModel(
            name='NetworkDevice',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('name', models.CharField(max_length=200)),
                ('device_type', models.CharField(choices=[('router', 'Router'), ('switch', 'Switch'), ('firewall', 'Firewall'), ('access_point', 'Access Point'), ('server', 'Servidor'), ('other', 'Otro')], max_length=50)),
                ('ip_address', models.GenericIPAddressField()),
                ('snmp_community', models.CharField(blank=True, max_length=100)),
                ('vendor', models.CharField(blank=True, max_length=100)),
                ('model', models.CharField(blank=True, max_length=100)),
                ('serial_number', models.CharField(blank=True, max_length=100)),
                ('is_monitored', models.BooleanField(default=True)),
                ('last_seen', models.DateTimeField(blank=True, null=True)),
                ('cpu_usage', models.FloatField(blank=True, null=True)),
                ('memory_usage', models.FloatField(blank=True, null=True)),
                ('uptime', models.BigIntegerField(blank=True, help_text='Uptime en segundos', null=True)),
                ('created_at', models.DateTimeField(auto_now_add=True)),
                ('updated_at', models.DateTimeField(auto_now=True)),
                ('site', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='devices', to='monitoring.site')),
            ],
        ),
        migrations.CreateModel(
            name='Alert',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('title', models.CharField(max_length=200)),
                ('description', models.TextField()),
                ('severity', models.CharField(choices=[('info', 'Informativa'), ('warning', 'Advertencia'), ('critical', 'Crítica')], max_length=20)),
                ('status', models.CharField(choices=[('active', 'Activa'), ('acknowledged', 'Reconocida'), ('resolved', 'Resuelta')], default='active', max_length=20)),
                ('detected_at', models.DateTimeField(auto_now_add=True)),
                ('acknowledged_at', models.DateTimeField(blank=True, null=True)),
                ('resolved_at', models.DateTimeField(blank=True, null=True)),
                ('acknowledged_by', models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.SET_NULL, related_name='acknowledged_alerts', to=settings.AUTH_USER_MODEL)),
                ('device', models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.CASCADE, related_name='alerts', to='monitoring.networkdevice')),
                ('site', models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.CASCADE, related_name='alerts', to='monitoring.site')),
            ],
            options={
                'ordering': ['-detected_at'],
            },
        ),
        migrations.CreateModel(
            name='Metric',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('metric_type', models.CharField(choices=[('cpu', 'Uso de CPU'), ('memory', 'Uso de Memoria'), ('bandwidth', 'Ancho de Banda'), ('latency', 'Latencia'), ('packet_loss', 'Pérdida de Paquetes'), ('temperature', 'Temperatura'), ('interface_status', 'Estado de Interfaz')], max_length=50)),
                ('value', models.FloatField()),
                ('unit', models.CharField(blank=True, max_length=20)),
                ('timestamp', models.DateTimeField(auto_now_add=True)),
                ('device', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='metrics', to='monitoring.networkdevice')),
            ],
            options={
                'ordering': ['-timestamp'],
                'indexes': [models.Index(fields=['device', 'metric_type', '-timestamp'], name='monitoring__device__fa1075_idx')],
            },
        ),
    ]
</file>

<file path="backend/monitoring/models.py">
from django.db import models
from django.contrib.auth.models import User


class Site(models.Model):
    """Representa una sede/ubicación física"""
    STATUS_CHOICES = [
        ('up', 'Operacional'),
        ('warning', 'Advertencia'),
        ('down', 'Inactivo'),
        ('unknown', 'Desconocido'),
    ]

    name = models.CharField(max_length=200)
    description = models.TextField(blank=True)
    address = models.CharField(max_length=500)
    city = models.CharField(max_length=100)
    country = models.CharField(max_length=100, default='España')
    latitude = models.DecimalField(max_digits=9, decimal_places=6, null=True, blank=True)
    longitude = models.DecimalField(max_digits=9, decimal_places=6, null=True, blank=True)

    # Información de red
    ip_range = models.CharField(max_length=100, blank=True, help_text="Ej: 192.168.1.0/24")
    vlan_id = models.IntegerField(null=True, blank=True)

    # Estado
    status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='unknown')
    last_check = models.DateTimeField(null=True, blank=True)

    # Contactos
    contact_name = models.CharField(max_length=100, blank=True)
    contact_phone = models.CharField(max_length=20, blank=True)
    contact_email = models.EmailField(blank=True)

    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        ordering = ['name']

    def __str__(self):
        return f"{self.name} ({self.get_status_display()})"


class NetworkDevice(models.Model):
    """Dispositivo de red (router, switch, firewall, etc.)"""
    DEVICE_TYPES = [
        ('router', 'Router'),
        ('switch', 'Switch'),
        ('firewall', 'Firewall'),
        ('access_point', 'Access Point'),
        ('server', 'Servidor'),
        ('other', 'Otro'),
    ]

    name = models.CharField(max_length=200)
    site = models.ForeignKey(Site, on_delete=models.CASCADE, related_name='devices')
    device_type = models.CharField(max_length=50, choices=DEVICE_TYPES)
    ip_address = models.GenericIPAddressField()
    snmp_community = models.CharField(max_length=100, blank=True)
    vendor = models.CharField(max_length=100, blank=True)
    model = models.CharField(max_length=100, blank=True)
    serial_number = models.CharField(max_length=100, blank=True)

    # Estado
    is_monitored = models.BooleanField(default=True)
    last_seen = models.DateTimeField(null=True, blank=True)

    # Métricas en tiempo real (cache)
    cpu_usage = models.FloatField(null=True, blank=True)
    memory_usage = models.FloatField(null=True, blank=True)
    uptime = models.BigIntegerField(null=True, blank=True, help_text="Uptime en segundos")

    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    def __str__(self):
        return f"{self.name} ({self.ip_address})"


class Metric(models.Model):
    """Métrica recogida de un dispositivo"""
    METRIC_TYPES = [
        ('cpu', 'Uso de CPU'),
        ('memory', 'Uso de Memoria'),
        ('bandwidth', 'Ancho de Banda'),
        ('latency', 'Latencia'),
        ('packet_loss', 'Pérdida de Paquetes'),
        ('temperature', 'Temperatura'),
        ('interface_status', 'Estado de Interfaz'),
    ]

    device = models.ForeignKey(NetworkDevice, on_delete=models.CASCADE, related_name='metrics')
    metric_type = models.CharField(max_length=50, choices=METRIC_TYPES)
    value = models.FloatField()
    unit = models.CharField(max_length=20, blank=True)
    timestamp = models.DateTimeField(auto_now_add=True)

    class Meta:
        ordering = ['-timestamp']
        indexes = [
            models.Index(fields=['device', 'metric_type', '-timestamp']),
        ]

    def __str__(self):
        return f"{self.device.name} - {self.get_metric_type_display()}: {self.value}{self.unit}"


class Alert(models.Model):
    """Alerta generada por el sistema"""
    SEVERITY_CHOICES = [
        ('info', 'Informativa'),
        ('warning', 'Advertencia'),
        ('critical', 'Crítica'),
    ]

    STATUS_CHOICES = [
        ('active', 'Activa'),
        ('acknowledged', 'Reconocida'),
        ('resolved', 'Resuelta'),
    ]

    title = models.CharField(max_length=200)
    description = models.TextField()
    severity = models.CharField(max_length=20, choices=SEVERITY_CHOICES)
    status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='active')

    site = models.ForeignKey(Site, on_delete=models.CASCADE, null=True, blank=True, related_name='alerts')
    device = models.ForeignKey(NetworkDevice, on_delete=models.CASCADE, null=True, blank=True, related_name='alerts')

    detected_at = models.DateTimeField(auto_now_add=True)
    acknowledged_at = models.DateTimeField(null=True, blank=True)
    resolved_at = models.DateTimeField(null=True, blank=True)
    acknowledged_by = models.ForeignKey(
        User, on_delete=models.SET_NULL, null=True, blank=True, related_name='acknowledged_alerts'
    )

    class Meta:
        ordering = ['-detected_at']

    def __str__(self):
        return f"[{self.get_severity_display()}] {self.title}"
</file>

<file path="backend/monitoring/serializers.py">
from rest_framework import serializers
from .models import Site, NetworkDevice, Metric, Alert


class SiteSerializer(serializers.ModelSerializer):
    device_count = serializers.SerializerMethodField()
    status_display = serializers.CharField(source='get_status_display', read_only=True)

    class Meta:
        model = Site
        fields = '__all__'
        read_only_fields = ['created_at', 'updated_at', 'last_check']

    def get_device_count(self, obj):
        return obj.devices.count()


class NetworkDeviceSerializer(serializers.ModelSerializer):
    site_name = serializers.CharField(source='site.name', read_only=True)
    device_type_display = serializers.CharField(source='get_device_type_display', read_only=True)

    class Meta:
        model = NetworkDevice
        fields = '__all__'
        read_only_fields = ['created_at', 'updated_at', 'last_seen']


class MetricSerializer(serializers.ModelSerializer):
    device_name = serializers.CharField(source='device.name', read_only=True)
    metric_type_display = serializers.CharField(source='get_metric_type_display', read_only=True)

    class Meta:
        model = Metric
        fields = '__all__'
        read_only_fields = ['timestamp']


class AlertSerializer(serializers.ModelSerializer):
    site_name = serializers.CharField(source='site.name', read_only=True)
    device_name = serializers.CharField(source='device.name', read_only=True)
    severity_display = serializers.CharField(source='get_severity_display', read_only=True)
    status_display = serializers.CharField(source='get_status_display', read_only=True)

    class Meta:
        model = Alert
        fields = '__all__'
        read_only_fields = ['detected_at', 'acknowledged_at', 'resolved_at', 'acknowledged_by']
</file>

<file path="monitoring-agents/netflow-agent/collector.conf">
# Minimal NetFlow collector config (nfdump / nfacct style)
listen: 2055
protocol: netflow_v5
storage:
  type: memory
  retention: 7d
outputs:
  prometheus:
    enabled: true
    listen: 9600
    metrics_path: /metrics
</file>

<file path="monitoring-agents/netflow-agent/Dockerfile">
FROM python:3.9-slim
WORKDIR /app
COPY collector.conf /app/collector.conf
# placeholder: install real collector (nfdump/nfcollector) in production
EXPOSE 9600/udp
CMD ["sh", "-c", "python -m http.server 9600"]
</file>

<file path="monitoring-agents/snmp-agent/Dockerfile">
FROM prom/snmp-exporter:latest
COPY snmp_exporter.yml /etc/snmp_exporter/snmp_exporter.yml
EXPOSE 9116
</file>

<file path="monitoring-agents/snmp-agent/README.md">
SNMP exporter minimal setup.

Env vars:
  SNMP_USER
  SNMP_AUTH_PASS
  SNMP_PRIV_PASS

Prometheus scrape target: snmp-agent:9116
Module examples: if_mib, snmp_v3
</file>

<file path="monitoring-agents/snmp-agent/snmp_exporter.yml">
modules:
  if_mib:
    walk:
      - 1.3.6.1.2.1.2   # IF-MIB
    version: 2
  snmp_v3:
    walk:
      - 1.3.6.1.2.1.1   # system
      - 1.3.6.1.2.1.2   # interfaces
    auth:
      username: "{{SNMP_USER}}"
      auth_protocol: SHA
      auth_password: "{{SNMP_AUTH_PASS}}"
      priv_protocol: AES
      priv_password: "{{SNMP_PRIV_PASS}}"
    version: 3
</file>

<file path="monitoring-agents/voip-agent/Dockerfile">
FROM python:3.9-slim
WORKDIR /app
COPY voip_exporter.yml /app/voip_exporter.yml
EXPOSE 9700
CMD ["sh", "-c", "python -m http.server 9700"]
</file>

<file path="monitoring-agents/voip-agent/voip_exporter.yml">
# Minimal VoIP exporter mapping (expects exporter exposing voip_* metrics)
metrics:
  - name: voip_mos_score
  - name: voip_jitter_ms
  - name: voip_latency_ms
scrape:
  listen: 9700
  path: /metrics
</file>

<file path="prometheus/alert_manager.yml">
global:
  resolve_timeout: 5m

route:
  receiver: 'team-email'

receivers:
  - name: 'team-email'
    webhook_configs:
      - url: 'http://your-webhook-or-notifier:8080/'
</file>

<file path="prometheus/alert_rules/alert_rules.yml">
groups:
  - name: core-alerts
    rules:
      - alert: InstanceDown
        expr: up == 0
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "Instance {{ $labels.instance }} down"
          description: "No metrics scraped from {{ $labels.instance }} for >2m."

      - alert: HighCPU
        expr: 100 - (avg by(instance) (irate(node_cpu_seconds_total{mode="idle"}[5m])) * 100) > 80
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High CPU on {{ $labels.instance }}"
          description: "CPU usage >80% for 5 minutes."

      - alert: VoIPQualityDegraded
        expr: voip_mos_score < 3.5 or voip_jitter_ms > 30 or voip_latency_ms > 200
        for: 3m
        labels:
          severity: critical
        annotations:
          summary: "VoIP quality degraded on {{ $labels.instance }}"
          description: "MOS <3.5 or jitter/latency above thresholds."
</file>

<file path="README.md">
# 🚀 Sistema de Monitorización de Red Corporativa Multisite

**Monitorización inteligente para redes empresariales de 200+ sedes**

[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://www.python.org/)
[![Django](https://img.shields.io/badge/Django-4.2-green.svg)](https://www.djangoproject.com/)
[![Grafana](https://img.shields.io/badge/Grafana-10.x-orange.svg)](https://grafana.com/)
[![Docker](https://img.shields.io/badge/Docker-Enabled-2496ED.svg)](https://www.docker.com/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

## 📋 **Descripción del Proyecto**

Sistema de monitorización centralizada para redes corporativas con múltiples sedes (200+). Proporciona visualización en tiempo real del estado de la red, conexiones VoIP, ancho de banda y disponibilidad de servicios críticos, diseñado específicamente para entornos corporativos como cadenas de supermercados u oficinas distribuidas.

### **🎯 Objetivo Principal**
Reducir el tiempo de respuesta ante incidencias de red en un 40% y mejorar el SLA de disponibilidad del 95% al 99.5%.

## ✨ **Características Principales**

### **🌍 Mapa Geográfico Interactivo**
- Visualización en tiempo real de todas las sedes con código de colores (verde/amarillo/rojo)
- Filtrado por región, tipo de sede o estado de conexión
- Información detallada al hacer clic en cada sede

### **🚨 Sistema de Alertas Inteligentes**
- Alertas por email, Telegram y Webhook
- Configuración de umbrales personalizados por tipo de métrica
- Escalado automático de alertas según criticidad
- Alertas predictivas basadas en patrones históricos

### **📊 Dashboards en Tiempo Real**
- Métricas de ancho de banda por sede y tipo de tráfico
- Monitoreo de calidad VoIP (MOS score, latencia, jitter)
- Disponibilidad de servicios críticos (DNS, ERP, Punto de Venta)
- Utilización de CPU/RAM de dispositivos de red

### **📈 Reportes Automáticos**
- Reportes diarios/semanales/mensuales de SLA
- Análisis de tendencias y crecimiento de tráfico
- Detección de cuellos de botella en la red
- Exportación a PDF, Excel y formato ejecutivo

### **🎫 Sistema de Tickets Integrado**
- Creación automática de tickets ante incidencias
- Seguimiento del ciclo de vida de problemas
- Base de conocimiento de soluciones recurrentes
- Integración con sistemas ITSM existentes

## 🛠️ **Stack Tecnológico**

### **Backend**
- **Python 3.8+** - Lógica principal del sistema
- **Django 4.2** - Framework web y API REST
- **Django REST Framework** - Construcción de API
- **Celery** - Tareas asíncronas y procesamiento en background
- **Redis** - Cache y broker de mensajes

### **Base de Datos**
- **PostgreSQL** - Base de datos principal
- **TimescaleDB** - Extensión para datos de series temporales
- **Redis** - Cache y sesiones

### **Monitorización y Métricas**
- **Prometheus** - Recolección y almacenamiento de métricas
- **Grafana** - Dashboards y visualizaciones
- **SNMPv3** - Protocolo para monitoreo de dispositivos
- **NetFlow/sFlow** - Análisis de tráfico de red

### **Frontend**
- **HTML5/CSS3/JavaScript ES6+**
- **Chart.js** - Gráficos interactivos
- **Leaflet.js** - Mapas interactivos
- **WebSocket** - Actualizaciones en tiempo real

### **Infraestructura**
- **Docker & Docker Compose** - Contenedores y orquestación
- **Nginx** - Reverse proxy y SSL
- **Gunicorn** - Servidor WSGI para Python
- **GitLab CI/CD** - Integración y despliegue continuo

## 📁 **Estructura del Proyecto**
multisite-network-monitor/
├── backend/ # Aplicación Django
│ ├── core/ # Configuración y settings
│ ├── monitoring/ # Lógica de monitorización
│ ├── alerts/ # Sistema de alertas
│ ├── reporting/ # Generación de reportes
│ ├── api/ # Endpoints REST
│ └── dashboard/ # Vistas y templates
├── frontend/ # Aplicación frontend moderna
│ ├── src/
│ │ ├── components/ # Componentes React/Vue
│ │ ├── views/ # Vistas principales
│ │ └── services/ # Conexión con API
├── monitoring-agents/ # Agentes para dispositivos de red
│ ├── snmp-agent/ # Agente SNMP
│ ├── netflow-agent/ # Agente NetFlow
│ └── voip-agent/ # Agente de calidad VoIP
├── prometheus/ # Configuración de Prometheus
│ ├── alert_rules/ # Reglas de alertas
│ └── scrape_configs/ # Configuración de scraping
├── grafana/ # Dashboards y configuración
├── docker/ # Configuración Docker
│ ├── compose.yaml # Docker Compose
│ └── nginx/ # Configuración Nginx
├── docs/ # Documentación
├── scripts/ # Scripts de utilidad
├── tests/ # Tests unitarios y de integración
└── config/ # Archivos de configuración

## 🚀 **Instalación Rápida**

### **Requisitos Previos**
- Docker y Docker Compose instalados
- Python 3.8+ (para desarrollo)
- Git

### **Instalación con Docker (Recomendada)**

```bash
# 1. Clonar el repositorio
git clone https://github.com/AlejandroGlezSan/multisite-network-monitor.git
cd multisite-network-monitor

# 2. Configurar variables de entorno
cp .env.example .env
# Editar .env con tus configuraciones

# 3. Iniciar los contenedores
docker-compose up -d

# 4. Acceder a la aplicación
# Dashboard principal: http://localhost:8000
# Grafana: http://localhost:3000 (admin/admin)
# API Documentation: http://localhost:8000/api/docs/
Instalación Manual para Desarrollo
bash
# 1. Crear entorno virtual
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate  # Windows

# 2. Instalar dependencias
pip install -r requirements/dev.txt

# 3. Configurar base de datos
python manage.py migrate
python manage.py createsuperuser

# 4. Cargar datos de prueba (opcional)
python manage.py loaddata fixtures/initial_data.json

# 5. Iniciar servidor de desarrollo
python manage.py runserver
📖 Guía de Uso
1. Agregar Sedes
python
# API Example: Crear nueva sede
POST /api/sites/
{
    "name": "Sede Central Madrid",
    "address": "Calle Gran Vía, 1",
    "latitude": 40.4168,
    "longitude": -3.7038,
    "ip_range": "192.168.1.0/24",
    "criticality": "high"
}
2. Configurar Dispositivos de Red
python
# Ejemplo de configuración YAML para switches
devices:
  - name: "Switch Piso 1"
    ip: "192.168.1.1"
    snmp_community: "monitoring_ro"
    device_type: "cisco_switch"
    metrics:
      - interface_traffic
      - cpu_utilization
      - memory_usage
3. Configurar Alertas
yaml
# Ejemplo de regla de alerta
alert_rules:
  - name: "Alta utilización de CPU"
    condition: "cpu_usage > 80"
    duration: "5m"
    severity: "warning"
    channels:
      - email: "noc@empresa.com"
      - telegram: "chat_id_123"
🔧 Configuración Avanzada
Monitoreo SNMP
python
# Configuración de monitoreo SNMP
SNMP_CONFIG = {
    'version': 3,
    'security_level': 'auth_with_privacy',
    'auth_protocol': 'SHA',
    'priv_protocol': 'AES',
    'community': 'monitoring_ro',
    'timeout': 5,
    'retries': 3
}
Integración con Sistemas Existentes
API REST completa para integración

Webhooks para eventos en tiempo real

Exportación a formatos estándar (CSV, JSON, XML)

Plugin para Nagios/Zabbix

         📊 Métricas y Resultados
KPIs Principales
MTTR (Mean Time To Repair): Reducción de 4 horas a 45 minutos

SLA de Disponibilidad: Mejora del 95% al 99.5%

Tiempo de Detección: De 30 minutos a < 2 minutos

Ahorro en Personal: 15 horas/semana por técnico

     Dashboard de Métricas
python
# Ejemplo de consulta para métricas clave
SELECT 
    site_name,
    AVG(uptime) as availability,
    MAX(bandwidth_usage) as peak_usage,
    COUNT(alerts) as incidents
FROM network_metrics
GROUP BY site_name
ORDER BY availability DESC;

    🤝 Contribuir al Proyecto

Fork el repositorio

Crea una rama para tu feature (git checkout -b feature/AmazingFeature)

Commit tus cambios (git commit -m 'Add AmazingFeature')

Push a la rama (git push origin feature/AmazingFeature)

Abre un Pull Request

Guía de Estilo
Sigue PEP 8 para código Python

Usa Black para formateo automático

Escribe tests para nuevas funcionalidades

Documenta todos los endpoints de API

📝 Licencia
Este proyecto está bajo la licencia MIT - ver el archivo LICENSE para más detalles.

📞 Soporte y Contacto
Issues: GitHub Issues

Email: alejandroglezsan1993@gmail.com

Documentación: Wiki del proyecto

🙏 Agradecimientos

Comunidad de Python y Django por el excelente ecosistema

Todos los contribuidores y testers del proyecto

⭐ Si este proyecto te resulta útil, por favor dale una estrella en GitHub!
</file>

<file path="backend/core/urls.py">
from django.contrib import admin
from django.urls import path, include
from rest_framework.routers import DefaultRouter
from monitoring import views

router = DefaultRouter()
router.register(r'sites', views.SiteViewSet)
router.register(r'devices', views.NetworkDeviceViewSet)
router.register(r'metrics', views.MetricViewSet)
router.register(r'alerts', views.AlertViewSet)

urlpatterns = [
    path('admin/', admin.site.urls),
    path('api/v1/', include(router.urls)),
    path('api-auth/', include('rest_framework.urls', namespace='rest_framework')),
]
</file>

<file path="backend/Dockerfile">
FROM python:3.9-slim-bullseye

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

# Dependencias del sistema necesarias para psycopg2 y compilación
RUN apt-get update && apt-get install -y \
    build-essential \
    libpq-dev \
    postgresql-client \
    curl \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Instalar dependencias Python
COPY requirements.txt .
RUN pip install --upgrade pip && pip install --no-cache-dir -r requirements.txt

# Copiar código y ajustar permisos
COPY . .
RUN addgroup --system app && adduser --system --ingroup app app \
    && chown -R app:app /app

USER app

EXPOSE 8000

HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \
  CMD curl -f http://localhost:8000/healthz || exit 1

# En producción setear DJANGO_PRODUCTION=1 y asegurar gunicorn en requirements.txt
CMD ["sh", "-c", "if [ \"$DJANGO_PRODUCTION\" = \"1\" ]; then gunicorn core.wsgi:application --bind 0.0.0.0:8000 --workers 3; else python manage.py runserver 0.0.0.0:8000; fi"]
</file>

<file path="backend/requirements.txt">
asgiref==3.11.0
Django==4.2.7
django-cors-headers==4.9.0
djangorestframework==3.14.0
psycopg2-binary==2.9.11
pytz==2026.1.post1
sqlparse==0.5.5
tzdata==2025.3
</file>

<file path="docker-compose.yml">
version: '3.8'

services:
  postgres:
    image: timescale/timescaledb:latest-pg14
    environment:
      POSTGRES_DB: network_monitor
      POSTGRES_USER: monitor
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL","pg_isready -U monitor"]
      interval: 30s
      retries: 5
    restart: unless-stopped

  backend:
    build: ./backend
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    depends_on:
      - postgres
    environment:
      POSTGRES_DB: network_monitor
      POSTGRES_USER: monitor
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
      POSTGRES_HOST: postgres
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
      - ./prometheus/alert_rules:/etc/prometheus/alert_rules
    ports:
      - "9090:9090"
    depends_on:
      - node_exporter
      - postgres_exporter
      - snmp-agent
    restart: unless-stopped

  grafana:
    image: grafana/grafana:latest
    environment:
      GF_SECURITY_ADMIN_PASSWORD: "${GF_SECURITY_ADMIN_PASSWORD}"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/dashboards:/etc/grafana/provisioning/dashboards
    ports:
      - "3000:3000"
    depends_on:
      - prometheus
    restart: unless-stopped

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    restart: unless-stopped

  alertmanager:
    image: prom/alertmanager:latest
    volumes:
      - ./prometheus/alertmanager.yml:/etc/alertmanager/alertmanager.yml
    ports:
      - "9093:9093"
    restart: unless-stopped

  node_exporter:
    image: prom/node-exporter:latest
    ports:
      - "9100:9100"
    restart: unless-stopped

  postgres_exporter:
    image: prometheuscommunity/postgres-exporter:latest
    environment:
      DATA_SOURCE_NAME: "postgresql://monitor:${POSTGRES_PASSWORD}@postgres:5432/network_monitor?sslmode=disable"
    ports:
      - "9187:9187"
    depends_on:
      - postgres
    restart: unless-stopped

  snmp-agent:
    build: ./monitoring-agents/snmp-agent
    ports:
      - "9116:9116"
    environment:
      SNMP_USER: "${SNMP_USER}"
      SNMP_AUTH_PASS: "${SNMP_AUTH_PASS}"
      SNMP_PRIV_PASS: "${SNMP_PRIV_PASS}"
    restart: unless-stopped

  netflow-agent:
    build: ./monitoring-agents/netflow-agent
    ports:
      - "9600:9600"
    restart: unless-stopped

  voip-agent:
    build: ./monitoring-agents/voip-agent
    ports:
      - "9700:9700"
    restart: unless-stopped

volumes:
  postgres_data:
  prometheus_data:
  grafana_data:
</file>

<file path="prometheus/prometheus.yml">
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert_rules/alert_rules.yml

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']
</file>

<file path="backend/core/settings.py">
"""
Django settings for core project - v2.0
"""
import os
from pathlib import Path
from dotenv import load_dotenv

BASE_DIR = Path(__file__).resolve().parent.parent

# Carga .env si existe (desarrollo local)
load_dotenv(BASE_DIR / '.env')

SECRET_KEY = os.getenv('DJANGO_SECRET_KEY', 'replace-me-in-production')
DEBUG = os.getenv('DJANGO_DEBUG', 'False').lower() in ('1', 'true', 'yes')

ALLOWED_HOSTS = os.getenv('ALLOWED_HOSTS', 'localhost,127.0.0.1').split(',')
CORS_ALLOWED_ORIGINS = os.getenv(
    'CORS_ALLOWED_ORIGINS',
    'http://localhost:3000,http://127.0.0.1:3000'
).split(',')

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    # Third party
    'corsheaders',
    'rest_framework',
    # Local apps
    'monitoring',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'corsheaders.middleware.CorsMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'core.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'core.wsgi.application'
DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Database (env-driven; en local usa .env con POSTGRES_HOST=localhost)
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': os.getenv('POSTGRES_DB', 'network_monitor'),
        'USER': os.getenv('POSTGRES_USER', 'monitor'),
        'PASSWORD': os.getenv('POSTGRES_PASSWORD', 'secure_password'),
        'HOST': os.getenv('POSTGRES_HOST', 'postgres'),
        'PORT': os.getenv('POSTGRES_PORT', '5432'),
    }
}

REST_FRAMEWORK = {
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 50,
    'DEFAULT_FILTER_BACKENDS': [
        'rest_framework.filters.SearchFilter',
        'rest_framework.filters.OrderingFilter',
    ],
}

STATIC_URL = '/static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'

LANGUAGE_CODE = 'es-es'
TIME_ZONE = 'Europe/Madrid'
USE_I18N = True
USE_TZ = True
</file>

</files>
